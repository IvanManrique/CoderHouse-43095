// declarar funciones
/*function saludoGeneral() {
    let nombre = prompt("Ingrese su nombre:");
    let sexo = prompt("Ingrese su sexo: (M/F)").toUpperCase();
    let saludo;

    // These if will define how the variable saludo will be fullfiled.

    if (sexo == "M") {
        saludo = "Bienvenido,";
    } else if (sexo == "F") {
        saludo = "Bienvenida, ";
    } else {
        saludo = "Bienvenide,";
    }

    alert(saludo + nombre);
    console.log("Hola, " + nombre + "!!!");
}
saludoGeneral(); */


/*function calculateIMC()
{
let peso = parseFloat(prompt("Ingrese su peso: (En Kg)"));
let altura = parseFloat(prompt("Ingrese su altura: (En mts)"));
let imc = peso / (altura*altura);

alert("Tu IMC es: " + imc.toFixed(2) + "%");

}
calculateIMC(); 


// Funciones con parámetros

function calculateIMC(peso, altura)
{
let imc = peso / (altura*altura);
alert("Tu IMC es: " + imc.toFixed(2) + "%");
}

let peso = parseFloat(prompt("Ingrese su peso: (En Kg)"));
let altura = parseFloat(prompt("Ingrese su altura: (En mts)"));
calculateIMC(peso, altura); 
*/
